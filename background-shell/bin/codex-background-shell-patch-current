#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec /usr/bin/env python3 "${ROOT_DIR}/scripts/codex_background_terminal_patch_current.py" "$@"
